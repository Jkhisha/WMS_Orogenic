@model IEnumerable<WareHouseMVC.Models.Region>

@{
    ViewBag.Title = "Index";
}

<div class="pagetitle">
    <h1>
      Index List</h1>
    <span>List of Indexs....</span>
</div>
<p>
    @Html.ActionLink("Create New", "Create", "", new { @class = "btn btn-primary" })
</p>

<div id="dyntable_wrapper" class="dataTables_wrapper" role="grid">
 <table class="table table-bordered" id="dyntable">
  <thead>
    <tr>
        <th></th>
        <th>
            RegionName
        </th>
    </tr>
	 </thead>
	  <tbody>
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink(" ", "Edit", new { id=item.RegionId }, new { @class = "icon-edit" }) |
            @Html.ActionLink(" ", "Details", new { id=item.RegionId }, new { @class = "icon-list-alt" }) |
            @Html.ActionLink(" ", "Delete", new { id=item.RegionId }, new { @class = "icon-trash" })
        </td>
        <td>
			@item.RegionName
        </td>
    </tr>
}
 </tbody>

</table>

</div>
